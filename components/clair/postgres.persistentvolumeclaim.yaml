apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: clair-postgres
  labels:
    quay-component: clair-postgres
spec:
  storageClassName: ceph-fs
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-fs